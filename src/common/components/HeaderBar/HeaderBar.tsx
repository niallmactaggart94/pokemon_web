import PokemonLogo from '../Logo/Logo';

const HeaderBar = () => {
	return (
		<header className="headerBar">
			<PokemonLogo className="pokemonLogoWrapper__logo" />
		</header>
	);
};

export default HeaderBar;
